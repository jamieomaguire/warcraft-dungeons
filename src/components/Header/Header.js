import React from 'react'

import styles from './Header.scss'

// A simple page header
const Header = () => (
  <header className={styles.header}>
    <h1 className={styles.title}>Classic WoW Dungeons</h1>
  </header>
)

export default Header
