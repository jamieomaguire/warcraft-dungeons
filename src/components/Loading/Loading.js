import React from 'react'

// A quick and simple message to assure people that the application is loading
const Loading = () => (
  <section aria-labelledby='Loading content'>
    <p>Loading the instances...</p>
  </section>
)

export default Loading
